# Keyless external-main-runtime composition. The owning snapshot applies the
# Provider's public Bundle patch and supplies the fixture executable argument.
- id: llm
  name: '@deepseek-ai/dsh-llm'

- id: session
  name: '@deepseek-ai/dsh-session'

- id: system-prompt
  name: '@deepseek-ai/dsh-system-prompt'

- id: tools
  name: '@deepseek-ai/dsh-tools'

- id: agent
  name: '@deepseek-ai/dsh-agent'

- id: agent-runtime
  name: '@deepseek-ai/dsh-agent-runtime'

- id: agent-runtime-profile
  name: '@deepseek-ai/dsh-agent-runtime-profile'
  config:
    defaultMainProfile: codex-main
    profiles:
      codex-main:
        provider: codex-app-server
        schemaVersion: 1
        launch:
          executable: !!js process.execPath
          args: !!js JSON.parse(process.env.DSH_CODEX_FIXTURE_ARGS ?? '[]')
          resolution: absolute
          cwdPolicy: session-workspace
          env:
            DSH_FIXTURE_EXIT_MARKER: .codex-exited
        product:
          kind: codex-app-server
        permissions:
          policy:
            sandbox: workspace-write
          enforcement: required
        process:
          startupTimeoutMs: 5000
          turnTimeoutMs: 5000
          shutdownTimeoutMs: 1000
          terminationTimeoutMs: 1000
          maxConcurrentRuns: 1

- id: subprocess
  name: '@deepseek-ai/dsh-subprocess-local'

- id: agent-runtime-launcher
  name: '@deepseek-ai/dsh-agent-runtime-launcher'

- id: agent-runtime-router
  name: '@deepseek-ai/dsh-agent-runtime-router'
